---
name: stanford-crfm-helm
description: "Holistic Evaluation of Language Models (HELM) is an open source Python framework created by the"
homepage: https://github.com/stanford-crfm/helm
metadata:
  openclaw:
    emoji: "⚙️"
    auto_generated: true
    requires:
      bins: ["crfm-helm", "go", "pip"]
---

# Stanford Crfm Helm

# Holistic Evaluation of Language Models (HELM)

## Usage

<!--quick-start-begin-->

Install the package from PyPI:

```sh
pip install crfm-helm
```

Run the following in your shell:

```sh

```
