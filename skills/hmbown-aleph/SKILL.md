---
name: hmbown-aleph
description: "Skill + MCP server to turn your agent into an RLM. Load context, iterate with search/code/think"
homepage: https://github.com/Hmbown/aleph
metadata:
  openclaw:
    emoji: "🔌"
    auto_generated: true
    requires:
      bins: ["git", "pip"]
---

# Hmbown Aleph

# Aleph

## Usage

1. Install Aleph:

```bash
pip install "aleph-rlm[mcp]"
```

2. Configure your MCP client:

```bash
aleph-rlm install
```

3. Verify Aleph is reachable in your assistant:

```text
get_status()

```
