---
name: modelcontextprotocol-go-sdk
description: "The official Go SDK for Model Context Protocol servers and clients. Maintained in collaboration"
homepage: https://github.com/modelcontextprotocol/go-sdk
metadata:
  openclaw:
    emoji: "🔧"
    auto_generated: true
    requires:
      bins: ["go"]
---

# Modelcontextprotocol Go Sdk

<!-- Autogenerated by weave; DO NOT EDIT -->

## Installation

To get started creating an MCP server, create an `mcp.Server` instance, add
features to it, and then run it over an `mcp.Transport`. For example, this
server adds a single simple tool, and then connects it to clients over
stdin/stdout:

```go
package main

import (
	"context"
	"log"

	"github.com/modelcontextprotocol/go-sdk/mcp"
)

type Input struct {
	Name string `json:"name" jsonschema:"the name of the person to greet"`
}

type Output struct {
	Greeting string `json:"greeting" jsonschema:"the



```
