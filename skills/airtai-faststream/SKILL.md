---
name: airtai-faststream
description: "FastStream is a powerful and easy-to-use asynchronous Python framework for building asynchronous"
homepage: https://github.com/ag2ai/faststream
metadata:
  openclaw:
    emoji: "🗄️"
    auto_generated: true
    requires:
      bins: ["pip"]
---

# Airtai Faststream

# FastStream

## Installation

**FastStream** works on **Linux**, **macOS**, **Windows** and most **Unix**-style operating systems.
You can install it with `pip` as usual:

```sh
pip install 'faststream[kafka]'



```
