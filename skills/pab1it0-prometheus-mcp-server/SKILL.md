---
name: pab1it0-prometheus-mcp-server
description: "A Model Context Protocol (MCP) server that enables AI agents and LLMs to query and analyze"
homepage: https://github.com/pab1it0/prometheus-mcp-server
metadata:
  openclaw:
    emoji: "🔌"
    auto_generated: true
    requires:
      bins: ["curl", "docker", "pip"]
---

# Pab1it0 Prometheus Mcp Server

# Prometheus MCP Server
