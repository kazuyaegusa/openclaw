---
name: trueleaf-apiflow
description: "A modern API workspace that works both online and offline — combining API documentation, testing,"
homepage: https://github.com/trueleaf/apiflow
metadata:
  openclaw:
    emoji: "🌐"
    auto_generated: true
    requires:
      bins: ["curl", "docker", "git", "npm"]
---

# Trueleaf Apiflow

<div align="center">

![logo](./docs/logo.jpg)

## Installation

1. **Clone the repository**

```bash
git clone https://github.com/trueleaf/apiflow.git
cd apiflow
npm install
```

2. **Start development servers**

```bash
npm run dev
```

This command will start both the frontend and backend servers concurrently:

- **Frontend (Web)**: http://localhost:4000
- **Backend (Server)**: http://localhost:7001
