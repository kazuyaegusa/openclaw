---
name: feature-engine-feature-engine
description: "Feature engineering and selection open-source Python library compatible with sklearn."
homepage: https://github.com/feature-engine/feature_engine
metadata:
  openclaw:
    emoji: "🔧"
    auto_generated: true
    requires:
      bins: ["feature_engine", "git", "pip"]
---

# Feature Engine Feature Engine

# Feature-engine

## Installation

From PyPI using pip:

```
pip install feature_engine
```

From Anaconda:

```
conda install -c conda-forge feature_engine
```

Or simply clone it:

```
git clone https://github.com/feature-engine/feature_engine.git
```
